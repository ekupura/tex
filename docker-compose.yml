version: '3'
services:
    tex:
        build: ./tex
        hostname: texuser
        container_name: tex
        volumes:
            -  ./volume:/mnt
        command: bash -c "cd /mnt/graduation-thesis/src && platex main.tex && dvipdfmx main.dvi"

